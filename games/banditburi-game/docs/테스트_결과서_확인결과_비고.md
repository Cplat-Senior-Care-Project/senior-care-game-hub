| QA 번호 | 확인 결과 | 비고 |
|---|---|---|
| D-01 | 통과 | `modes/standard`, `modes/reminder`, `modes/care`, `modes/ai-assisted` 4개 모드 폴더가 모두 존재함. |
| D-02 | 통과 | 각 모드에 `index.html`, `css/game.css`, `js/game.js`, `config/game.config.json`가 모두 존재함. |
| D-03 | 통과 | Node JSON 파서로 5개 `game.config.json` 모두 정상 파싱 확인. |
| D-04 | 통과 | `loadGameConfig()`가 `config/game.config.json?v=${Date.now()}`를 동기 로드하여 캐시를 무효화하고 inline config와 병합함. |
| D-05 | 통과 | 공통 `runtimeConfig` 필드는 모드별 inline/config에 정의되어 있으며 기본값도 `MODE_DEFAULTS`에 존재함. |
| D-06 | 통과 | `show_timer` 값에 따라 타이머 stat 영역을 `setElementVisible()`로 표시/숨김 처리함. |
| D-07 | 통과 | `show_score` 값에 따라 진행/남은 개수 stat 영역을 표시/숨김 처리함. |
| D-08 | 통과 | `show_difficulty_select=false`이면 `showDifficultyScreen()`에서 난이도 모달을 건너뛰고 시작 카운트다운으로 이동함. |
| D-09 | 부분 통과 | 설정 버튼/화면은 `show_settings`로 제어됨. 다만 care/ai-assisted inline config는 `show_settings:true`라 설정 화면이 표시됨. |
| D-10 | 통과 | `show_how_to_play`에 따라 인트로/일시정지 내 게임 방법 버튼이 표시/숨김 처리됨. |
| D-11 | 통과 | `show_condition_check`가 true일 때 시작 전 체크인 화면으로 진입하고, false이면 난이도/시작 플로우로 넘어감. |
| D-12 | 통과 | `show_finish_check`가 true일 때 종료 후 체크인 화면으로 연결되고, false이면 결과 화면으로 바로 이동함. |
| D-13 | 통과 | `hint_enabled` 값으로 힌트 버튼 표시 여부를 제어함. |
| D-14 | 통과 | `auto_hint_enabled` true일 때 `scheduleAutoHint()`가 자동 힌트 타이머를 예약함. |
| D-15 | 통과 | `voice_guide_enabled`가 `voiceEnabled` 초기값에 반영되고 음성 재생 함수에서 게이트 처리됨. |
| D-16 | 통과 | `high_contrast` 값으로 `body.mode-high-contrast` 클래스가 토글되고 관련 CSS가 적용됨. |
| D-17 | 통과 | 표준 모드 `question_count`가 10으로 설정되어 `maxRounds`에 반영됨. |
| D-18 | 통과 | 알림 모드는 `auto_start:true`, `auto_return:true`가 설정되어 자동 시작/복귀 로직을 사용함. |
| D-19 | 통과 | 케어 모드는 4문항, 2x2, 목표 1개, 총 180초 설정이 적용됨. |
| D-20 | 통과 | care와 ai-assisted의 게임 로직 파일 해시가 동일하며, 모드명/설정 차이만 있음. |
| D-21 | 통과 | EASY/low는 2x2, targetCount 2로 정의됨. |
| D-22 | 통과 | NORMAL/medium은 3x3, targetCount 3으로 정의됨. |
| D-23 | 통과 | HARD/high는 4x4, targetCount 4로 정의됨. |
| D-24 | 부분 통과 | HARD의 무궁화는 `targetPool()`에서 6라운드부터 포함됨. config의 `flowerStartsFromRound` 값은 직접 참조하지 않고 코드 상수 로직으로 처리됨. |
| D-25 | 통과 | 정답 위치는 shuffle된 인덱스 배열을 slice 후 Set으로 저장하여 중복 생성되지 않음. |
| D-26 | 통과 | 정답 인덱스는 `0..total-1` 범위에서 생성되고 targetCount는 전체 셀 수 이하로 제한됨. |
| D-27 | 통과 | 정답 셀에는 targetType, 나머지 셀에는 distractorPool 기반 방해 요소가 배치됨. |
| D-28 | 통과 | 정답 판정은 위치 Set 기준이며 테마 이미지는 `applyTheme()`/CSS 표시만 변경함. |
| D-29 | 통과 | 무궁화는 별도 targetType으로 선택되고 안내 문구/이미지/방해 요소와 구분됨. |
| D-30 | 통과 | `startRound()`에서 chosenCorrect/chosenWrong/chosenWrongTypes와 힌트 상태를 매 라운드 초기화함. |
| D-31 | 통과 | 보드 `data-grid-size`와 CSS 변수 `--grid-size`를 현재 gridSize로 설정함. |
| D-32 | 확인 필요 | CSS에서 2x2/3x3/4x4 이미지 크기 보정 규칙은 존재하나, 실제 브라우저/기기 시각 확인은 필요함. |
| D-33 | 통과 | 시작 전 카운트다운 메시지와 보드 미리보기 흐름이 `beginStartCountdown()`/`startPreviewCountdown()`에 구현됨. |
| D-34 | 확인 필요 | 전구/새/휴대폰/무궁화별 크기 보정 CSS는 있으나, 실제 렌더링 균일성은 시각 확인 필요. |
| D-35 | 확인 필요 | 보드 배경/테두리 CSS는 정의되어 있으나 이미지와의 겹침 여부는 뷰포트별 시각 확인 필요. |
| D-36 | 확인 필요 | 카드/정답/선택 테두리 스타일은 분리되어 있으나 실제 충돌 여부는 시각 확인 필요. |
| D-37 | 통과 | 미리보기 단계에서 셀 버튼이 disabled 처리되고 `chooseCell()`도 `isPreviewing`이면 즉시 반환함. |
| D-38 | 통과 | 선택은 `roundActive && !isPreviewing && !isPaused` 조건에서만 가능함. |
| D-39 | 부분 통과 | 셀 선택은 click 기반이며 `touch-action: manipulation`은 있음. `touchcancel`/`pointercancel` 전용 복구 로직은 없음. |
| D-40 | 통과 | 선택 즉시 correct/wrong 클래스와 이미지/aria-label이 갱신됨. |
| D-41 | 통과 | hover/focus-visible 스타일과 coarse pointer 예외 CSS가 존재함. |
| D-42 | 통과 | 선택한 정답 수가 targetIndexes 크기에 도달하면 `finishRound(success)`로 다음 단계 이동. |
| D-43 | 통과 | 이미 선택된 correct/wrong 셀은 조기 반환하고 오답은 3회 제한으로 처리함. |
| D-44 | 통과 | 힌트는 targetIndexes 자체를 변경하지 않고 미선택 정답만 표시하므로 이후 정답 선택 로직이 유지됨. |
| D-45 | 통과 | 표준/알림 모드는 라운드 제한 시간이 감소하며 `updateTimeDisplay()`로 화면에 표시됨. |
| D-46 | 통과 | 케어/AI 모드는 `show_timer:false`, `round_time_limit_sec:0`으로 UI 타이머는 숨기고 총 제한 시간 로직은 유지함. |
| D-47 | 통과 | 총 180초 제한 만료 시 `handleTotalTimeExpired()`를 통해 결과 전환 로직이 실행됨. |
| D-48 | 통과 | 케어/AI 모드는 `auto_hint_delay_sec:40` 설정과 자동 힌트 예약 로직이 있음. |
| D-49 | 통과 | 자동 힌트 타이머는 라운드 시작/힌트 종료 시 clear 후 1회 재예약됨. |
| D-50 | 통과 | 자동 힌트 팝업은 `showAssistNotice()` 후 `showHint()`로 이어지며 게임 진행을 유지함. |
| D-51 | 통과 | 코드에 매핑된 효과음/음성 MP3 파일 경로가 실제 파일로 존재함. |
| D-52 | 통과 | 표준 모드 설정 화면 음성은 `settings.mp3`로 매핑됨. |
| D-53 | 통과 | 테마 선택 음성은 `theme-select.mp3`로 매핑됨. |
| D-54 | 통과 | 튜토리얼 5단계 음성(`tutorial-intro/preview/choose/hint/total`)이 매핑 및 파일 존재함. |
| D-55 | 통과 | 시작 전 컨디션 체크 안내/건너뛰기 음성(`pre-checkin`, `skip-pre-checkin`)이 존재함. |
| D-56 | 통과 | 종료 후 체크 안내 음성(`finish-status-select`, `post-help-question`)이 존재함. |
| D-57 | 통과 | 점수화면 안내 음성 `score-screen.mp3`가 매핑 및 존재함. |
| D-58 | 통과 | 케어/AI 전용 assist 음성 7종이 매핑 및 파일 존재함. |
| D-59 | 통과 | “불이 켜지는 곳을 같이 볼까요?” 문구/음성은 실제 게임 코드에서 사용되지 않음. |
| D-60 | 통과 | 정답/오답 선택 시 `playCorrectSound()`/`playWrongSound()`가 각각 호출됨. |
| D-61 | 통과 | `wrong.mp3` 파일은 있으나 현재 오답 효과음 경로로 호출되지 않음. |
| D-62 | 통과 | `getScoreSummary()`에서 정답수/오답수/정답률을 계산하고 점수 화면에 표시함. |
| D-63 | 통과 | 점수화면 On이면 종료 멘트 후 점수 확인 버튼/점수 화면으로 진입함. |
| D-64 | 통과 | 점수화면 Off이면 `showResultsScreen()`으로 바로 이동함. |
| D-65 | 확인 필요 | 모바일 점수 카드 반응형 CSS는 다수 존재하나 실제 세로/가로 뷰포트 시각 확인 필요. |
| D-66 | 통과 | 시작 전 컨디션 데이터는 `condition.before`에 mood/sleepDurationGroup으로 포함됨. |
| D-67 | 통과 | 종료 후 컨디션 데이터는 `condition.after`에 mood/perceivedDifficulty/fatigue/neededHelp/wantsReplay로 포함됨. |
| D-68 | 통과 | 건너뛰기 시 null 기본값이 기록되도록 payload 기본 구조가 있음. |
| D-69 | 통과 | 완료 시 `GAME_COMPLETED`, `status:'completed'` payload가 생성됨. |
| D-70 | 통과 | 중도 종료 시 `GAME_ABANDONED`, `status:'abandoned'`, abandonedReason이 생성됨. |
| D-71 | 부분 통과 | correct_count/wrong_count와 accuracy는 계산됨. 단 필드명 `accuracy_rate`는 없고 `accuracy`/`metrics.roundAccuracy`로 기록됨. |
| D-72 | 부분 통과 | 문항별 로그는 `question_logs`로 생성되며 `result_detail_json` 내부가 아니라 payload 최상위에 위치함. |
| D-73 | 통과 | 문항별 `first_response_time_ms`와 `response_time_ms`가 기록됨. |
| D-74 | 문제 발견 | `changed_answer_count` 필드는 존재하지만 항상 0으로 고정되고 선택 변경 기능 자체가 없음. |
| D-75 | 부분 통과 | 오터치 성격의 `wrong_tap_count`는 기록됨. `drag_fail_count`는 필드만 있고 0 고정이며 별도 touch_miss 지표는 없음. |
| D-76 | 부분 통과 | `hint_count`/`hint_used`는 기록됨. 수동/자동 힌트 구분 플래그는 별도 기록되지 않음. |
| D-77 | 통과 | 전체 소요시간(`duration_ms`, `total_elapsed_ms`)과 문항별 `response_time_ms`가 기록됨. |
| D-78 | 통과 | `GAME_READY`, `GAME_STARTED`, `GAME_COMPLETED`가 WebView postMessage로 전달됨. |
| D-79 | 부분 통과 | 알림 모드 auto_return 로직은 있음. 다만 URL이 로컬 `file:///C:/...`로 하드코딩되어 배포 환경 수정 필요. |
| D-80 | 부분 통과 | 케어/AI 복귀 버튼 로직은 있음. `return_url`이 로컬 `file:///C:/...` 경로라 배포 환경 수정 필요. |
| D-81 | 통과 | 마이크/STT 권한 요청 코드는 없고, 외부 브리지 텍스트/명령 수신만 처리함. |
| D-82 | 통과 | `parseBridgeMessage()`, `handleBridgeMessage()`, `handleExternalAnswer()`로 앱 전달 명령 처리 지점이 분리됨. |
| D-83 | 확인 필요 | 표준 모드 웹 레이아웃 반응형 CSS는 있으나 좌측 패널/보드/안내 멘트 균형은 브라우저 시각 확인 필요. |
| D-84 | 확인 필요 | 알림 모드 웹 배치 CSS는 있으나 보드/안내 멘트 겹침 여부는 시각 확인 필요. |
| D-85 | 확인 필요 | 케어/AI 모드 보드 확대 CSS는 있으나 충분한 크기인지는 실제 화면 확인 필요. |
| D-86 | 확인 필요 | 모바일 세로에서 보드/하단 패널을 맞추는 CSS는 있으나 실제 기기 확인 필요. |
| D-87 | 확인 필요 | 케어/AI 세로 모드 return 버튼 위치 CSS는 있으나 하단 중앙 표시 여부는 시각 확인 필요. |
| D-88 | 확인 필요 | 모바일 가로 표준/알림 배치 CSS는 있으나 의도한 좌측 패널/중앙 보드 배치는 시각 확인 필요. |
| D-89 | 확인 필요 | 케어/AI 가로 모드 return 버튼 위치 CSS는 있으나 지정 위치 표시 여부는 시각 확인 필요. |
| D-90 | 확인 필요 | 튜토리얼 모달 반응형 CSS는 있으나 버튼/설명 문구 겹침은 시각 확인 필요. |
| D-91 | 부분 통과 | 주요 영역과 일부 버튼에 aria-label/role이 있음. 셀 aria-label은 생성되지만 모든 컨트롤의 스크린리더 문맥은 추가 점검 권장. |
| D-92 | 통과 | `:focus-visible` 스타일이 CSS에 정의되어 키보드 포커스 표시가 사라지지 않음. |
| D-93 | 부분 통과 | 정답/오답/힌트는 색상 외 클래스, 이미지, 문구도 병행됨. 다만 시각 대비/두께는 실제 접근성 확인 권장. |
| D-94 | 확인 필요 | 케어/AI는 `flash_effect_level:low` 설정이 있고 CSS 효과가 있으나 실제 반복 플래시 강도는 시각 확인 필요. |
| D-95 | 통과 | 음성 재생 실패 시 catch/대체 타이밍 처리로 게임 진행이 중단되지 않음. |
| D-96 | 부분 통과 | 이미지 alt는 제공되지만 이미지 로드 실패 시 대체 텍스트 카드로 전환하는 `onerror` 핸들러는 없음. |
| D-97 | 통과 | config fetch/parse 실패 시 try/catch로 inline config와 `MODE_DEFAULTS` 기반 진행이 가능함. |
| D-98 | 확인 필요 | 모바일 보드 전환/음성 재생 지연은 코드만으로 확정 불가하며 실제 기기 성능 확인 필요. |
| D-99 | 통과 | 세션 초기화 시 타이머/이벤트 상태를 clear하고 셀은 재생성되므로 반복 플레이 시 중복 등록 위험이 낮음. |
| D-100 | 부분 통과 | 모드 URL 구조는 `modes/*/index.html`로 배포 가능함. config/return URL에 로컬 `file:///C:/...` 경로가 남아 있어 배포 전 수정 필요. |
